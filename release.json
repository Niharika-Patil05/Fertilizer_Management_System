{
  "version": "1.2.0",
  "notes": "Replaced the hardcoded default admin login with a first-run 'Create Your Account' screen (the old open /register endpoint is now locked after the first account). Added a one-click installer (Install Fertilizer Shop.bat) that installs Docker automatically if needed and creates a 'Fertilizer Shop' Desktop icon, so day-to-day use is just double-clicking that icon."
}
